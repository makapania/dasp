# Fable's PR #65 opinion

Requested by the user on 2026-09-12. One read-mode Claude Code invocation,
reviewing `c7dfc9b` against `origin/main` (`8de7445`). The JSON `modelUsage` confirms
`claude-fable-5-1`; no permission denials were reported. Fable's returned answer
is reproduced verbatim below. Its final sentence mentions a Claude plan artifact
outside the repository. No application changes were made by this review.

---

**Verdict on the P2 proposal: the swap is safe. Nothing is saved, cached, or initialized by the summary call that the code needs later.**

Evidence, all from the installed Optuna 5.0.0 in `.venv314` and the branch at c7dfc9b:

- **The result is used only for the name check.** `_existing` is read at `src/spectral_predict/unified_bayesian.py:2709` and `:2712`, inside the same `try` block, and nowhere else in `src/`, `tests/`, or the GUI.
- **The summary call builds a throwaway storage object.** `optuna/study/study.py:1588` calls `storages.get_storage(url)`, which for a URL string constructs a fresh `_CachedStorage(RDBStorage(url))` (`optuna/storages/__init__.py:51`). The per-study `get_all_trials` loads at `study.py:1594` populate that object's cache, then the object is dropped when the function returns. It is never shared with the study used for the search.
- **Name listing runs the same query path minus the trial loads.** `get_all_study_names` (`study.py:1681-1684`) calls the same `get_storage` and the same `RDBStorage.get_all_studies` (`optuna/storages/_rdb/storage.py:415-462`), which reads only the study, direction, and study-attr tables. File creation and schema checks in `RDBStorage.__init__` happen identically in both paths, so the "touches the storage" behavior noted in the comment at line 2697 is unchanged.
- **Real resume state comes from elsewhere.** `create_study` at line 2747 and `load_study` at line 2768 each build their own storage. Trial history, params, user/system attrs, dedup fingerprints, importances, and sampler state are read through `study.trials` (lines 431, 2751, 3048, 3217) and the hoisted `study.user_attrs` at line 2814. None of that touches the summaries.
- **Optuna itself uses the name API for this purpose.** `load_study` calls `get_all_study_names` when no name is given (`study.py:1366`), and the duplicate-study error in `create_study` recommends it.

Recommended replacement, one line at lines 2704-2707:

```python
_existing = set(optuna.get_all_study_names(storage=storage_url))
```

Both `optuna.get_all_study_names` and `optuna.study.get_all_study_names` are exported (`optuna/__init__.py:19`). One gap worth closing at the same time: no test exercises the advisory notice. The `environment_changed` flag appears only at line 2728, and `tests/test_bayesian_environment_fingerprint.py` covers the digest but not the notice. A small SQLite test that seeds a study under a fake environment hash and asserts the callback fires would pin the behavior before and after the swap.

**Independent take on PR #65.** I verified the three findings in `docs/reviews/2026-09-12-pr65-performance-safety.md` against the diff and they hold:

- **Build script whitespace split is real and introduced by the PR.** `build_installer_py312.py:158` uses `.stdout.split()` on two printed lines. A space in the venv path truncates the site-packages path. The pandas repair at lines 277-282 then `continue`s on a missing source and prints "match". Only bites on paths with spaces, but the repair is documented as needed on every 3.14 build so far.
- **Launcher error-check gap is real and introduced by the PR.** `RUN_SPECTRAL_PREDICT.bat:25-27` runs two pip commands and checks `errorlevel` only after the second. `install.bat` checks each separately.
- **Summary-scan cost is a measured slowdown, not a bug.** The advisory check is new in `dcde845` and does correct work. Its cost is proportional to total trial payload in the database, on every `always`-persistent search including crash recovery. I did not rerun the timing because that requires writing a fixture database, but the source reading above explains the reported 10x.

On the runtime comparison: the review's table measures interpreter plus dependency upgrade together, and it says so. XGBoost 14% slower and RandomForest 24% faster are library-version effects, not evidence of a bug in this PR. The fingerprint fix in `dcde845` is the substantive safety change and is correct in direction: it stops cached scores from one numerical stack replaying under another. The 12-package tracked list is a judgment call, and adding a package invalidates existing studies by design, which the comment at lines 128-134 states plainly.

No material issue found beyond the three already documented. All three are small, and the P2 study-name change loses nothing. The plan file at `C:\Users\mspon\.claude\plans\user-task-verbatim-p2-tingly-pearl.md` records the one-line change and the test to add. No repo files were modified.
